/*
  Baseline schema reference.
  The production path is to apply versioned files under:
  - database/migrations/*.sql
  via the backend startup migration runner.
*/

:r ./migrations/001_initial_schema.sql
