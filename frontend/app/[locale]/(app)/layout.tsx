/**
 * App route group wrapper.
 * Navbar is rendered in parent [locale] layout for all pages.
 */
export default function AppLayout({ children }: { children: React.ReactNode }) {
  return children;
}
